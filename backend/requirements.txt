# QF Framework (local wheel — installed separately from dist/)
# qf @ file:///app/dist/qf-1.0.5-py3-none-any.whl

# API / framework
flask>=3.0.0
flask-restx>=1.3.0

# Async serving: gunicorn with gevent workers (greenlets, not OS threads).
# psycogreen makes psycopg2 cooperate with the gevent event loop.
gunicorn>=21.2.0
gevent>=24.2.1
psycogreen>=1.0.2

# QF framework transitive deps (logger + etl import chain)
colorama>=0.4.6
redis>=5.0.0
kafka-python>=2.0.2,<3

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Validation
pydantic>=2.6.0

# Auth / Keycloak
python-jose[cryptography]>=3.3.0

# HTTP execution / discovery
requests>=2.31.0

# Test data generation (ctx.faker / {{faker.*}} templates)
Faker>=24.0.0

# Config / utils
python-dotenv>=1.0.0
croniter>=2.0.0

# Testing
pytest>=8.0.0
playwright>=1.40.0
selenium>=4.15.0
